TagReplacer.js - A simple function to find HTML elements of a specified type and replace them with something else.

Requirements:
jQuery and the getAttributes plugin.
http://jquery.com/
http://plugins.jquery.com/project/getAttributes

Usage:
All you need to do is call replaceTags() with an associative array as the argument.
Example: replaceTags({tagToReplace:"replaceWith", p:"div", b:"span"});

Enjoy! :)
SphereCat1